#include<bits/stdc++.h>
using namespace std;
#define int long long
signed main() {
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int d;
    cin >> d;
    if (d >= 12)
        cout << 'v' << endl;
    else
        cout << 'w' << endl;
    return 0;
}